% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sisal.R
\name{sisal}
\alias{sisal}
\title{Title sisal}
\usage{
sisal(
  Y,
  p,
  iters = 80,
  tau = 1,
  mu = p * 1000/ncol(Y),
  spherize = F,
  tol = 0.01,
  m0 = NULL,
  verbose = F,
  returnPlot = T,
  nonNeg = F
)
}
\arguments{
\item{Y}{A numeric matrix of size L x N representing the gene expression data, where L is the number of genes and N is the number of samples.}

\item{p}{The number of tissue types/components to estimate. This should be a positive integer less than or equal to L (the number of genes).}

\item{iters}{An integer specifying the number of iterations for the optimization process (default is 80).}

\item{tau}{A regularization parameter controlling the sparsity of the solution (default is 1).}

\item{mu}{A regularization parameter influencing the stability of the solution (default is p * 1000 / ncol(Y)).}

\item{spherize}{A logical value indicating whether to apply spherization to the data (default is FALSE).}

\item{tol}{A numeric value specifying the convergence tolerance (default is 1e-2).}

\item{m0}{An optional initial matrix for tissue proportions. If \code{NULL}, the matrix is computed using the VCA method (default is NULL).}

\item{verbose}{A logical value indicating whether to print progress messages during the iterations (default is FALSE).}

\item{returnPlot}{A logical value indicating whether to return a plot of the iteration steps (default is TRUE).}

\item{nonNeg}{A logical value indicating whether to enforce non-negativity constraints on the solution (default is FALSE).}
}
\value{
A list containing:
\describe{
\item{endpoints}{A matrix of estimated tissue-specific components (endpoints), with columns representing the tissue types and rows representing the genes.}
\item{endpointsProjection}{A matrix of projections of the data onto the tissue-specific components.}
\item{projection}{A matrix of the estimated tissue proportions (projection matrix).}
\item{shift}{A vector representing the mean-centered data.}
\item{singValues}{A vector of singular values from the singular value decomposition (SVD).}
\item{distances}{A vector of distances from each sample to the estimated tissue components (endpoints).}
\item{reduced}{The reduced data matrix after dimensionality reduction.}
\item{q}{The final transformation matrix used in the optimization process.}
\item{plotObj}{Optional: A data frame containing the plot of iteration steps, if \code{returnPlot} is TRUE.}
}
}
\description{
#' SISAL: Sparse Inverse Simultaneous Component Analysis
}
\details{
This function performs Sparse Inverse Simultaneous Component Analysis (SISAL) for tissue deconvolution.
It estimates tissue-specific gene expression profiles (endpoints) and the corresponding tissue proportions.
}
\examples{
# Example usage of the sisal function
data <- matrix(rnorm(1000), nrow = 100, ncol = 10) # Random example data (100 genes x 10 samples)
result <- sisal(Y = data, p = 3, iters = 50, tau = 1, mu = 300)
}
